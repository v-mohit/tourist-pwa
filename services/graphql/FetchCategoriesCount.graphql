query FetchCategoriesCounts {
  categories {
    data {
      id
      attributes {
        Name
        icon {
          data {
            attributes {
              url
            }
          }
        }
        places(pagination: { limit: 100 }) {
          data {
            id
          }
        }
      }
    }
  }
}